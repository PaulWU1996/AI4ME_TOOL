{
  "_comment": "X-API-Key below is a placeholder -- the http driver treats auth as static, externally-provisioned context (it has no key-generation/rotation logic of its own, unlike worker/utils.py:ensure_api_key). Regenerate with: curl -X POST http://<visualservice-host>:8000/api/keys/generate -H 'X-Admin-Key: <AI4ME_ADMIN_PASSWORD>' -H 'Content-Type: application/json' -d '{\"client_name\": \"full_http\"}' -- then paste the returned api_key below.",
  "workflow": {
    "name": "full_http",
    "version": "1.0"
  },
  "settings": {
    "on_failure": "stop",
    "parallel": true
  },
  "tasks": [
    {
      "id": "download",
      "task": "download_file",
      "call": "kwargs",
      "inject": [
        "path",
        "prompts",
        "job_id"
      ],
      "requires": [
        "path"
      ],
      "depends_on": [],
      "retries": 3
    },
    {
      "id": "visual",
      "driver": "http",
      "url": "http://visualservice:8000/analyze",
      "file_field": "video",
      "file_path_key": "file_path",
      "headers": {
        "X-API-Key": "REPLACE_WITH_VALID_VISUALSERVICE_API_KEY"
      },
      "timeout": 6000,
      "service": "visualservice",
      "depends_on": ["download"]
    },
    {
      "id": "audio",
      "driver": "http",
      "url": "http://audioservice:8000/process_audio/",
      "timeout": 1800,
      "service": "audioservice",
      "depends_on": ["download"]
    }
  ]
}
