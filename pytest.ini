[pytest]
testpaths = tests
pythonpath = .
addopts = -ra --strict-markers
markers =
    gap: documents a known, unfixed gap — paired with xfail(strict=True) so the test turns into a failure the day the gap is closed, forcing the marker to be removed.
    slow: uses real sleeps to observe concurrency.
